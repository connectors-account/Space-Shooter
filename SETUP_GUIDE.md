# Setup Guide

For the full and latest setup instructions (scene hierarchy, prefab setup, sprite specs, controls, and Windows build pipeline), use:

- `README.md`

This repository is configured for Unity 2022.3 LTS and Windows desktop standalone builds.
